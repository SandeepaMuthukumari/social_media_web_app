apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-media-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: social-media-web-app
  template:
    metadata:
      labels:
        app: social-media-web-app
    spec:
      containers:
        - name: social-web-media-app
          image: sandeepadocker/social-media-web-app-pipeline:1.0.0-12  # e.g., public.ecr.aws/your-image
          ports:
            - containerPort: 3000
